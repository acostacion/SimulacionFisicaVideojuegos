#include "ParticleSystem.h"

void ParticleSystem::update(double t)
{
	//for (auto p : _particles) p->integrate(t);

	// _particles->integrate()
	// elimina particulas
	// crea particulas _generators->generatePC()
}
